' action and motion of attributes easy level of measure
' create as level produzed the markets of logical level
' make attributes easy in scales of measure
Declare Function CreateLevel () As Integer

#if __fb_mt__
        #print Using multi-threaded library
#else
        #print Using single-threaded library
#endif

End
