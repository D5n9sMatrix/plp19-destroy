' section of measure level logical to make distribution
' template level measure view method section dirs view
' make distrubutes.
Declare Function SunHotWill () As Integer

'' Set option explicit always on

#ifdef __fb_lang__
  #if __fb_lang__ <> "fb"
    Option Explicit
  #endif
#else
  '' Older version - before lang fb
  Option Explicit
#endif

End